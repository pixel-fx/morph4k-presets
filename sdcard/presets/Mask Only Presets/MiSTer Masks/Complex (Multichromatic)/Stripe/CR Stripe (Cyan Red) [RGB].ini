[main]
orig_preset_name = CR Stripe (Cyan Red) [RGB].ini
display_name = CR Stripe (Cyan Red) [RGB]
description = Description can go here
tags = Comma separated list of tags, e.g. N64,320x240
; orig_feature_mask = 0x00000200

[slotmask]
intensity = 1
conversion = none
data = <<EOF
vMFX
# mode: 1
# offset: 0,0

# w,h
2,1

# lut
0x689898,0x986868
EOF
